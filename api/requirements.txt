fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1
sqlalchemy==2.0.42
alembic==1.13.0
python-jose[cryptography]==3.5.0
python-multipart==0.0.20
passlib==1.7.4
bcrypt==4.3.0
python-dateutil==2.8.2
fastapi-users[sqlalchemy]==14.0.1
fastapi-users[oauth]==14.0.1
httpx-oauth>=0.16.1
httpx==0.28.1
msal==1.31.0  # Microsoft Authentication Library for Azure AD SSO
setuptools==80.9.0

# PostgreSQL driver
psycopg2-binary==2.9.10

# Email service dependencies
boto3==1.40.1  # AWS SES
azure-communication-email==1.0.0  # Azure Email Services
requests==2.32.4  # For Mailgun HTTP API
jinja2==3.1.6  # Email templating
reportlab==4.4.3  # PDF generation for email attachments

# AI/LLM dependencies
openai==1.68.2  # OpenAI client (compatible with litellm)
litellm==1.75.0  # Unified interface for multiple LLM providers
langchain==0.3.27  # LangChain for PDF processing
langchain-community==0.3.27  # LangChain community components
pypdf==6.0.0  # Modern PDF processing (successor to PyPDF2)
ollama==0.5.1  # Ollama client
ollama-ocr==0.1.6  # OCR processor (still needed for invoice processing)

# Security fix for HTTP Request Smuggling vulnerability
h11==0.16.0

# Test dependencies
pytest==8.4.1
pytest-asyncio==1.1.0
pytest-mock==3.14.1
pytest-cov==6.2.1

# Image processing
pillow==11.3.0

# Excel export
openpyxl==3.1.2

# Cron scheduling
croniter==3.0.3

# Kafka
confluent-kafka==2.10.0

# Redis for caching
redis==5.2.1
hiredis==3.1.0

# OpenSearch
opensearch-py==2.4.2

pymupdf>=1.26.0 # not directly required, pinned by Snyk to avoid a vulnerability
transformers>=4.53.0 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.5.0 # not directly required, pinned by Snyk to avoid a vulnerability
