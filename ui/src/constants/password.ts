/**
 * Password-related constants for the UI
 * These are fetched from the API at runtime to ensure consistency
 * Password requirements are dynamically loaded via authApi.getPasswordRequirements()
 */

// This file is kept for reference - actual password requirements come from the API
// The API endpoint /auth/password-requirements provides the current configuration
